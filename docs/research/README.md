# Research Notes

Discovery notes for features that need investigation before implementation.
Each file captures why the behavior matters, relevant code paths inspected,
and decisions that narrowed implementation options.

Only create a research note when a feature is too uncertain for a single pass.
Small, well-understood changes don't need one.
